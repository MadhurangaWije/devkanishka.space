# Resend (https://resend.com) is used by the /ask (Ask Me) page to email
# questions to hi@devkanishka.space and send the asker a confirmation copy.
# Sign up, verify the devkanishka.space sending domain (Resend gives you the
# DNS records to add), then create an API key and set it below.
RESEND_API_KEY=
